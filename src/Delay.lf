// Logical delay.
target JavaScript;
actor Delay {
   input in:object;
   output out:object;
   parameter delay:number;
   trigger trig:object;
   reaction(in) {=
      schedule(trig, delay, get(in));
   =}
   reaction(trig) -> out {=
      set(out, trig);
   =}
}