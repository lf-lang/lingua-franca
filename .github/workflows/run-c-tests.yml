name: C Tests

on:
  workflow_call:
    inputs:
      runtime-ref:
        required: false
        type: string
      use-cpp:
        required: false
        type: boolean
jobs:
  run-tests:
    name: Run C tests
    strategy:
      matrix:
        platform: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Install JDK ${{ inputs.runtime-ref }}
        uses: actions/setup-java@v1.4.3
        with:
          # The Java version to make available on the path.
          # Takes a whole or semver Java version, or 1.x syntax (e.g. 1.8 => Java 8.x). 
          # Early access versions can be specified in the form of e.g. 14-ea, 14.0.0-ea, or 14.0.0-ea.28
          java-version: 11
      - uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0 # NOTE: if this parameter is not used, fetching an alternative runtime fails.
      - name: Use alternative runtime version
        run: |
          pushd org.lflang/src/lib/c/reactor-c
          git fetch --all
          git checkout ${{ inputs.runtime-ref }}
          popd
        if: ${{ inputs.runtime-ref }}
      - name: Install dependencies Ubuntu
        run: sudo apt-get install libprotobuf-c-dev protobuf-c-compiler protobuf-compiler libprotobuf-dev
        if: runner.os == 'Linux'
      - name: Install dependencies OS X
        run: |
          brew install protobuf
          brew install protobuf-c
          brew install coreutils
        if: runner.os == 'macOS'
      - name: Install RTI
        uses: ./.github/actions/install-rti
        if: ${{ runner.os == 'macOS' || runner.os == 'Linux' }}
      - name: Perform C tests
        run: |
          ./gradlew test --tests org.lflang.tests.runtime.CTest.*
        if: ${{ !inputs.use-cpp }}
      - name: Perform CCpp tests
        run: |
          ./gradlew test --tests org.lflang.tests.runtime.CCppTest.*
        if: ${{ inputs.use-cpp }}