package org.lflang.pretvm.register;

/** An external variable to store the start time of the application in epoch time */
public class StartTime extends GlobalRegister {
  public StartTime() {
    super();
  }
}
