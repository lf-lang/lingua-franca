target C {
    timeout: 1 sec,
    coordination: decentralized
}

import Platform from "Database.lf"

federated reactor ReplicatedDatabase(STP:time(30 msec)) {
    a = new Platform();
    b = new Platform();
    a.publish -> b.update;
}
