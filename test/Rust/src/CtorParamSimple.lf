target Rust;

reactor Print(value:i32(42)) {
    state v: i32(value);

    reaction(startup) {=
        assert_eq!(23, params.value);
        assert_eq!(self.v, params.value);
    =}
}
main reactor CtorParamSimple {
    p = new Print(value=23);
}
