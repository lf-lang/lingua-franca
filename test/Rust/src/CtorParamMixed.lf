target Rust;

reactor Print(
    value: i32(42),
    name: String({="xxx".into()=}),
    other: bool(false)
) {
    reaction(startup) {=
        assert_eq!(42, params.value);
        assert_eq!("x2hr", params.name.as_str());
        assert_eq!(true, params.other);
    =}
}
main reactor CtorParamMixed {
    p = new Print(other=true, name={="x2hr".into()=});
}
