target Rust

reactor Foo(param: Vec<f64> = {= vec![2.0, 3.0, 4.0] =}) {
  state param = param

  reaction(startup) {=
    println!( "{}, {}, {}", self.param[0], self.param[1], self.param[2]);
  =}
}

main reactor {
  f = new Foo(param = {= vec![2.2, 3.3, 4.4] =})
  f_ = new Foo()
}
