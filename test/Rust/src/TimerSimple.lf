target Rust;
main reactor TimerSimple {
    state i: i32(0);
    timer t;
    reaction(t) {=
        println!("Tick {} after {} ms", self.i, ctx.get_elapsed_physical_time().as_millis());
        self.i += 1;
    =}
}
