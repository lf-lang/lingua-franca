target Rust

// this thing must compile needs to be modified when
main reactor TypeVarLengthList {
  state l0: Vec<i32> = {= Vec::new() =}  // generates l0: Vec::new()
  state l1: Vec<i32> = {= vec![1, 2] =}
  state l2: {= [i32; 2] =} = [1, 2]
}
