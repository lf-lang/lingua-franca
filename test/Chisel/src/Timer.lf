target Chisel {
    timeout: 10 usec
};

main reactor {
    timer t(0, 1 usec)
    reaction(t) {=
      val logicalNow = lf_time_logical();
      val physicalNow = lf_time_physical();
      printf("Timer triggered @ logical time: %d physical time: %d\n", logicalNow, physicalNow);
    =}
}