target Cpp {
  logging: error,  // To test generating a custom trace file name.
  docker: true,
  build-type: Debug
}

import HelloWorld2 from "../HelloWorld.lf"

main reactor {
  a = new HelloWorld2()
}
