/**
 * Test that the link-libs target property is working correctly.
 * The failure for this test is failure to compile.
 */
target Cpp {
    cmake-include: "include/dummy_lib/CMakeLists.txt",
    link-libs: "DUMMY_LIB",
    timeout: 0 sec
};
public preamble {=
    #include "dummy.h"
=}
main reactor {
    reaction(startup) {=
        dummy_function();
    =}
}
