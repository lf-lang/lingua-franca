target Cpp

import Count from "../lib/Count.lf"

reactor Print {
    input in: int
    state c: int(1)

    reaction(in) {=  =}

    reaction(shutdown) {=  =}
}

federated reactor {
    c = new Count()
    p = new Print()
    c.c -> p.in after 100 msec
}
