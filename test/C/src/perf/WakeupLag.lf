target C

main reactor {
  timer t(0, 100 msec)

  reaction(t) {=
    instant_t now = lf_time_physical_elapsed();
    interval_t lag = now - lf_time_logical_elapsed();
    lf_print("%llu", lag);
  =}
}
