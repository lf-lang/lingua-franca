target C {
    platform: {
        name: Zephyr,
        board: qemu_cortex_m3
    },
    threading: false
}

preamble {=
    #include <stdio.h>
=}

main reactor {
    timer t(0, 1 sec)

    reaction(t) {= printf("Hello\n"); =}
}
