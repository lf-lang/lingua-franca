target C {
    platform: "Zephyr",
    build: "bash scripts/zephyr_build.sh qemu_cortex_m3 flash",
    threading: false,
    cmake-include: "scripts/zephyr.cmake"
}

main reactor {
    reaction(startup) {=
        printf("Hello World!\n");
    =}
}