target C {
    platform: "Zephyr",
    build: "bash scripts/zephyr_build.sh qemu_cortex_m3 flash",
    threading: false,
}

main reactor {
    reaction(startup) {=
        printf("Hello World!\n");
    =}
}