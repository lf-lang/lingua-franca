target C {
  platform: "Patmos",
  single-threaded: true,
  // Skip physical busy-waits; required for cycle-accurate patemu in CI.
  fast: true
}

main reactor {
  reaction(startup) {=
    printf("Hello World!\n");
  =}
}
