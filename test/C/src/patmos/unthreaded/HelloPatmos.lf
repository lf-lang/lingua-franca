target C {
  platform: "Patmos",
  single-threaded: true,
  fast: true  // Skip physical busy-waits; required for cycle-accurate patemu in CI.
}

main reactor {
  reaction(startup) {=
    printf("Hello World!\n");
  =}
}
