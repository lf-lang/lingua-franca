// This is a smoke test of a minimal reactor.
target C {
  threading: false,
  platform: "Patmos",
  build: "echo run make"
}

main reactor PatmosMinimal {
    reaction(startup) {= printf("Hello World.\n"); =}
}
