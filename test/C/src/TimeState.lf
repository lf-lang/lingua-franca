target C

preamble {=
    #include <stdio.h>
=}

reactor Foo(bar: int(42)) {
    state baz: time(500 msec)

    reaction(startup) {= printf("Baz: %lld\n", self->baz); =}
}

main reactor {
    a = new Foo()
}
