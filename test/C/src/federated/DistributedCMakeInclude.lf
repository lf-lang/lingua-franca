/**
 * Test that cmake-include in an included file is being imported correctly.
 * The failure for this test is failure to compile.
 */
target C {
    timeout: 0 sec
};

import Foo from "../target/CMakeInclude.lf"

main reactor {
    f1 = new Foo();
    f2 = new Foo();
}
