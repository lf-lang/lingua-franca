target Python

reactor Count(offset(0), period(1 sec)) {
    output out

    timer t(offset, period)

    state count(1)

    reaction(t) -> out {=
        out.set(self.count)
        self.count += 1
    =}
}
