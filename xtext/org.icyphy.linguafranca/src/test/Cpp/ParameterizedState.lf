target Cpp;

reactor Foo(bar:int(42)) {
    state baz(bar);
    reaction (startup) {=
        std::cout << "Baz: " << baz << std::endl;
    =}
}
main reactor ParameterizedState {
    a = new Foo();
}
