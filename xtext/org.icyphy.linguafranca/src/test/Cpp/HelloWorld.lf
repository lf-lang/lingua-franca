target Cpp;
reactor HelloWorldInside {
    preamble {=
        #include <iostream>
    =}
    timer t;
    reaction(t) {=
        std::cout << "Hello World." << std::endl;
    =}
}
main reactor HelloWorld {
    a = new HelloWorldInside();
}
