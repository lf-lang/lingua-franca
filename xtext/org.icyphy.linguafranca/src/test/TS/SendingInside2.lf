target TypeScript;
reactor Printer {
    input x:number;
    reaction(x) {=
        console.log("Inside reactor received:" + x );
    =}
}
main reactor SendingInside2 {
    timer t;
    p = new Printer();
    reaction(t) -> p.x {=
        p.x = 1;
    =}
}