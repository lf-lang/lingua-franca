target TypeScript;
reactor Printer {
    input x:number;
    reaction(x) x {=
        console.log("Inside reactor received:" + x.get() );
    =}
}
main reactor SendingInside2 {
    timer t;
    p = new Printer();
    reaction(t) -> p.x {=
        p.x.set(1);
    =}
}