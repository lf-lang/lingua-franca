target TypeScript;

reactor Foo(bar:number(42)) {
    state baz:time(500 msec);
    
    reaction (startup) {=
        console.log("Baz: " + baz.get());
    =}
}

main reactor X {
    a = new Foo();
}