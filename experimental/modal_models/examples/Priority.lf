target Python;

reactor Priority {
	input a;
	input b;
	
	state o;
    
    initial mode WaitAB {
		reaction(a) -> A {=
            self.o += 1
            set_mode(A)
		=}
		reaction(b) -> B {=
            self.o += 1
            set_mode(B)
		=}
    }
    mode A {
    }
    mode B {
    }
}