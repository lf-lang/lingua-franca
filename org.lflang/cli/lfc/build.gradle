plugins {
    id 'org.lflang.java-application'
}

dependencies {
    implementation project(':org.lflang:cli:base')
}

application {
    mainClass = 'org.lflang.cli.Lfc'
    tasks.run.workingDir = rootProject.projectDir
}
