module org.lflang.GenerateLinguaFranca

import org.eclipse.xtext.xtext.generator.*
import org.eclipse.xtext.xtext.generator.model.project.*

var rootPath = ".."
var baseName = "org.lflang"
var epochBuild = false

Workflow {
    component = XtextGenerator {
        configuration = {
            project = StandardProjectConfig {
                baseName = baseName
                rootPath = rootPath
                runtime = {
                    enabled = true
                    name = baseName // generate into the same project
                }
                genericIde = {
                    enabled = true
                    name = baseName // generate into the same project
                }
                // Tests are generated manually
                runtimeTest = {
                    enabled = false
                }
                // Only generate Eclipse infrastructure for the Epoch build (separate repository).
                createEclipseMetaData = epochBuild
                eclipsePlugin = {
                    enabled = epochBuild
                }
                eclipsePluginTest = {
                    enabled = epochBuild
                }
            }
            code = {
                encoding = "UTF-8"
                lineDelimiter = "\n"
                fileHeader = "/*\n * generated by Xtext \${version}\n */"
                preferXtendStubs = false
            }
        }
        language = StandardLanguage {
            name = "org.lflang.LinguaFranca"
            fileExtensions = "lf"
            serializer = {
                generateStub = false
            }
            validator = {
                composedCheck = "org.eclipse.xtext.validation.NamesAreUniqueValidator"
                //composedCheck = "org.eclipse.xtext.validation.ImportUriValidator"
                // Generates checks for @Deprecated grammar annotations, an IssueProvider and a corresponding PropertyPage
                generateDeprecationValidation = true
                generateXtendStub = false
            }
            junitSupport = {
                junitVersion = "5"
                generateXtendStub = false
            }
            parserGenerator = {
                debugGrammar = true
            }
            generator = {
                generateXtendMain = false
            }
            formatter = {
                generateStub = true
            }
            projectWizard = {
                generate = false
            }
        }
    }
}
