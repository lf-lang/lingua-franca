[package]
name = "$crate.name"
version = "$crate.version"
authors = ["$crate.author0"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
reactor-rust = { $runtime.toml_spec }
