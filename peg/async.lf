reactor HTTPGet () {
  input url:string;
  output out:string;
  action arrived:string;
  reaction(url) {=
    httpRequest(url , function(reply) {
      schedule(arrived , reply);
    });
  =}
  reaction(arrived) -> out {=
    set(out , get(arrived));
  =}
}
